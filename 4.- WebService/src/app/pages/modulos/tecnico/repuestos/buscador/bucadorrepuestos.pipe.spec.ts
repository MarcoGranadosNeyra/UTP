import { BucadorrepuestosPipe } from './bucadorrepuestos.pipe';

describe('BucadorrepuestosPipe', () => {
  it('create an instance', () => {
    const pipe = new BucadorrepuestosPipe();
    expect(pipe).toBeTruthy();
  });
});
